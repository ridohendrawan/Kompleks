<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="avanger.co.id">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UAS_Avanger">
        <provider
            android:authorities="avanger.co.id.fileprovider"
            android:name="androidx.core.content.FileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
        <activity android:name="avanger.co.id.DetailDaftarTamu"></activity>
        <activity android:name="avanger.co.id.PanduanPengguna" />
        <activity android:name="avanger.co.id.DaftarTamu" />
        <activity android:name="avanger.co.id.FormBaru" />
        <activity android:name="avanger.co.id.MainMenu" />
        <activity android:name="avanger.co.id.LupaPassword" />
        <activity android:name="avanger.co.id.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="avanger.co.id.MainActivity" />
    </application>

</manifest>